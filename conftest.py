import pytest
#from selene import browser, have, be #для работы с браузером (например закрытие и прочее, это по сути встроенные команды и функции для работы с браузером)
#from selenium import webdriver


#@pytest.fixture(scope="function", autouse=True)
#def browser_management():
    #browser.config.base_url = 'https://todomvc.com/examples/emberjs'

    #yield

    #browser.quit()